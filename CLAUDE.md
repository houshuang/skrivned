# Skrivned

## Build & Install

Always use `./build.sh` to build and install. It builds release, copies to `~/.local/bin/`, and ad-hoc signs the binary (required for macOS accessibility permissions to persist across rebuilds).
